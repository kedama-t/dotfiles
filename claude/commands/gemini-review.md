Bash tool を使って Gemini CLI にコードレビューを依頼してください。

$ARGUMENTS で指定されたファイルの内容を Gemini に渡してレビューさせます。

実行するコマンド:
```
cat $ARGUMENTS | gemini -p "以下のコードをレビューしてください。バグ、改善点、セキュリティ上の問題があれば指摘してください。"
```

Gemini からの回答をそのままユーザーに表示してください。
